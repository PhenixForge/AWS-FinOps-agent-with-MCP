# Placeholder marking a possible future direction, not active work: this
# project could one day grow a GCP-agnostic equivalent of the 3 FinOps tools
# (Cost Explorer -> Cloud Billing, CloudWatch -> Cloud Monitoring, etc).
#
# Explicitly out of scope for now — see "Explicitement hors scope" in
# finops-mcp-agent.md for why (doubling clouds doubles auth/IAM friction that
# copilotage doesn't compress, and GCP is meant to be learned on Valeo office
# hours, not personal time). No GCP resources are defined here; this file is
# intentionally empty of Terraform code.
